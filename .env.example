# Frontend Environment
# Copy to .env.local for the Next.js frontend only.

# Public backend address. Safe to expose to the browser.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080

# Optional. If omitted, WebSocket URL is derived from NEXT_PUBLIC_API_BASE_URL.
NEXT_PUBLIC_WS_BASE_URL=
